/* ============================================================
   firebase-messaging-sw.js — STUB
   ============================================================
   FCM background messages are handled entirely by sw.js.
   notifications.js passes sw.js to getToken() via
   serviceWorkerRegistration, so FCM uses sw.js directly.

   This file must exist at the root to satisfy FCM's fallback
   check, but it does nothing — all real logic is in sw.js.
   ============================================================ */
'use strict';
